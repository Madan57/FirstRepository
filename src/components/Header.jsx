import React from "react";

const Header = () => {
  return (
    <div>
      <p>
        i'm header component added by madan branch to test merge from madan into
        main/master branch lets go
      </p>
    </div>
  );
};

export default Header;
